import { StyleSheet } from "react-native";

export const menuStyles = StyleSheet.create({

    // PLAY

    containerPlay: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }

})